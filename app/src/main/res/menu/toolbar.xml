<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- "Mark Favorite", should appear as action button if possible -->
    <item
        android:id="@+id/action_send"
        android:icon="@drawable/ic_send"
        android:title="@string/action_send"
        app:showAsAction="always"/>
    <item
        android:id="@+id/action_reco1"
        android:icon="@drawable/ic_recorrido1"
        android:title="@string/action_reco1"
        app:showAsAction="always"/>
    <item
        android:id="@+id/action_reco2"
        android:icon="@drawable/ic_recorrido2"
        android:title="@string/action_reco2"
        app:showAsAction="always"/>

    <!-- Settings, should always be in the overflow -->
    <item android:id="@+id/action_extra"
        android:title="@string/action_info"
        app:showAsAction="never"/>

</menu>